// app.js
// Ponto de entrada do JavaScript da aplicação.
// A lógica do board Kanban (kanban.js) e a camada de API (api.js)
// serão adicionadas a partir da Fase 2/3.

document.addEventListener('DOMContentLoaded', () => {
    console.log('Kanban App inicializado.');
});